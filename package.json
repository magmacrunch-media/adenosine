{
  "name": "adenosine",
  "version": "0.2.0",
  "private": true,
  "description": "A collection of web game engines by MagmaCrunch",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=20"
  },
  "homepage": "https://magmacrunch.com/ware/adenosine/",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/magmacrunchmedia/adenosine.git"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "oxlint",
    "coverage": "npm run test:coverage --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "check": "node scripts/check-packaging.mjs && node scripts/check-publish-resolution.mjs && node scripts/check-no-hardcoded-hosts.mjs && node scripts/check-api-docs.mjs && node scripts/check-css-fallbacks.mjs && node scripts/check-cdn-pins.mjs && node scripts/check-bundle-size.mjs"
  },
  "devDependencies": {
    "@arethetypeswrong/cli": "^0.18.5",
    "@vitest/coverage-v8": "^4.1.11",
    "oxlint": "^1.80.0",
    "publint": "^0.3.24"
  }
}
